server.port=8085

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/quizmgmt}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:root123}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:kumarshahil5279@gmail.com}
spring.mail.password=${MAIL_PASSWORD:sahuyypesbnjbepb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Gemini API Configuration (AI Features)
# Replace the value below with your actual API key from Google AI Studio
gemini.api.key=${GEMINI_API_KEY:AIzaSyAkUXjBQohMTUzdTjOltaJcQBYaVGcK2qg}

# File Upload Config
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB